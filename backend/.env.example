# ──────────────────────────────────────────────────────────────
# ApexJudge Backend – environment variables
# Set these in docker-compose.yml or your host environment.
# ──────────────────────────────────────────────────────────────

# Supabase JWT secret  (Project Settings → API → JWT Secret)
# Used by /api/v1/auth/supabase-login to verify Supabase access tokens
SUPABASE_JWT_SECRET=your-supabase-jwt-secret-here

# Local JWT secret (used for app-internal tokens after login)
JWT_SECRET_KEY=apexjudge-dev-secret-key-change-in-prod
